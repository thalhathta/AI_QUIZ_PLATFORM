# --- App ---
NODE_ENV=development
PORT=4000

# --- MongoDB ---
# If you have a db name, prefer keeping it in the URI itself; MONGODB_DB_NAME is optional.
MONGODB_URI=mongodb://127.0.0.1:27017/ai_quiz_platform
MONGODB_DB_NAME=

# --- Auth / JWT (to be used later) ---
JWT_SECRET=replace_me_with_a_long_random_string
JWT_EXPIRES_IN=7d

# --- Claude / Anthropic ---
CLAUDE_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Pick a valid model you have access to; default in code is claude-3-5-sonnet-20240620
CLAUDE_MODEL=

# --- CORS ---
# Comma-separated list. Use * ONLY for development.
CORS_ORIGIN=http://localhost:5173

# --- Rate limiting ---
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# --- Logging / Misc ---
LOG_LEVEL=info
